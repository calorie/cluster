#!/usr/bin/env ruby

begin
  require 'cluster'
  require 'cluster/cli'
rescue
  require 'rubygems'
  require 'cluster'
  require 'cluster/cli'
end

Cli.source_root(File.join(File.dirname(__FILE__), '..'))
Cli.start
